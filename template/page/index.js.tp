import './index.less';

import React, { Component } from 'react';
import { observer } from 'mobx-react';

// import <%= this.user_inputs.page_name %>Controller from '@controllers/<%= this.user_inputs.page_name %>Controller';

@observer
class <%= this.user_inputs.page_name %>Page extends Component {
  render() {
    return (
      <div className={'<%= this.user_inputs.page_name %>'}>

      </div>
    );
  }
}

export default <%= this.user_inputs.page_name %>Page;