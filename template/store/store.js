import { observable, computed, autorun } from 'mobx';

export default class _ControllerName_ {
  // 监听属性示例
  // @observable userList = [];

  // 计算属性示例
  // @computed get first_user() {
  //   return userList[0];
  // } 
};

// 衍生：自动运行函数示例
// autorun(() => {
//   const arr = _ControllerName_.userList;
//   console.log(arr);
// });